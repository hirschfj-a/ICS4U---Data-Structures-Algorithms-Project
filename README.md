(I am going to start coding Version 3.2.0 by changing the code so that the linked lists are separate. I separated the classes into their own files in Version 3.1.3)

Version 3.2.0 has two instances of the same linked list class, and each class is separated into their own files
